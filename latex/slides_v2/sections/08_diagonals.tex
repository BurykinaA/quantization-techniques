% ─── Slide 8a: Diagonals — fix + before/after picture ────────────────────────
\begin{frame}{Our Extension II: Trainable Diagonals (\texttt{add\_diag})}

  \footnotesize
  Absorb a trainable diagonal $D=\mathrm{diag}(d_1,\ldots,d_c)$ into the rotation:
  \[
    \tilde T \;=\; T\cdot D,\qquad d_i \in [10^{-4},\, 10]
  \]
  Invariance preserved — opposite scaling on the next layer:
  \[
    y \;=\; (xD^{-1}T^{-1})\,(T D W^\top).
  \]

  \vspace{0.6em}
  \begin{center}
    \fbox{\parbox[c][0.32\textheight][c]{0.92\linewidth}{%
      \centering
      \textcolor{myorange}{\large\textbf{TODO: before / after picture}}\\[0.4em]
      \scriptsize activation distribution before and after diagonal scaling (horizontal)
    }}
  \end{center}
\end{frame}

% ─── Slide 8b: Diagonals — placement + ablation table ────────────────────────
\begin{frame}{Our Extension II: Trainable Diagonals — Placement \& Ablation}

  \begin{columns}[T]
    \begin{column}{0.46\textwidth}
      \footnotesize
      \textbf{Where we place them.}
      \begin{itemize}\setlength\itemsep{0.4em}
        \item \texttt{diag\_attn}: before \texttt{q/k/v/o} projections\\
              {\scriptsize attention-head variance}
        \item \texttt{diag\_mlp}: before \texttt{gate/up/down}\\
              {\scriptsize SwiGLU outliers}
      \end{itemize}
    \end{column}

    \begin{column}{0.50\textwidth}
      \scriptsize
      \textbf{Ablation (Llama-3.2-1B, w4a4, $\alpha{=}0.5$, ADC PPL$\downarrow$):}\\[0.4em]
      \renewcommand{\arraystretch}{1.25}
      \begin{tabular}{@{}l c c@{}}
        \toprule
        \textbf{$\alpha$-mix base $+$} & \bypass & \adcppl{} \\
        \midrule
        — (no diag)              & 24.24 & 31.22 \\
        \texttt{diag\_mlp} only  & \textbf{19.37} & 31.65 \\
        \texttt{diag\_attn} only & 24.98 & 29.41 \\
        both diagonals           & 20.23 & \best{27.56} \\
        \bottomrule
      \end{tabular}
    \end{column}
  \end{columns}
\end{frame}
